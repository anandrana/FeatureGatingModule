package utils;

public class EnumList {
    public enum Gender {Male, Female}
    public enum PreferredModeOfPayment {COD, Wallet, CreditCard, DebitCard}
    public enum CustomerType {PRIME, SUPREME, NORMAL}
    public enum AccountType {ACTIVE, BLOCKED, CLOSED}
    public enum OperatorType {AllOf, Between, ExactlyEqualTo, GreaterThan, GreaterThanEqual, LessThan, LessThanEqual, LogicalAND, LogicalNOT, LogicalOR, NoneOf }
}
